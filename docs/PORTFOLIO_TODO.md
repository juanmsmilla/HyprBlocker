# Portfolio Polish — Remaining Work

Items from the portfolio review that still need doing, roughly in order of impact.

## 1. Screenshots & demo (highest impact — only you can do this)

The README has a placeholder block waiting for these (`<!-- SCREENSHOTS -->` near the top).

- [ ] Capture: Dashboard, block configuration modal, Statistics page, the blocked page in a browser, tray menu
- [ ] Save as `docs/screenshots/*.png` and uncomment the block in README.md
- [ ] Ideally: a short GIF/webm of navigating to a blocked site and getting stopped (e.g. `wf-recorder` + `ffmpeg -i out.mkv -vf "fps=12,scale=800:-1" demo.gif`)
- [ ] Consider setting the GIF as the GitHub social preview image (repo Settings → Social preview)

## 2. Commit and push the current changes

Nothing from this polish pass has been committed yet. Suggested grouping:

- [ ] `fix: match subdomains for domain-only patterns in daemon (parity with extension)` — `daemon/blocker.py` + tests
- [ ] `chore: add ruff config and fix lint across daemon, apps, and frontend`
- [ ] `ci: add GitHub Actions workflow (ruff, pytest, frontend build)`
- [ ] `docs: rewrite README, add LICENSE, watchdog and technical docs`

Going forward, use descriptive conventional-style commit messages — reviewers do skim `git log`.

## 3. Testing depth — DONE

Current suite covers the pure logic (pattern matching, scheduling). Worth adding:

- [x] **API tests** with FastAPI's `TestClient` + an in-memory SQLite database: block CRUD, lock-mode rejection (403 when locked), `/api/blocks/{id}/strict` only accepts *stricter* rules (`tests/test_blocks_api.py`)
- [x] **Extension logic tests** — extracted `matchesPattern`/`matchesPatternWithPath` to `extension/matcher.js` (loaded via `importScripts`), tested with `bun test extension` (`extension/matcher.test.js`, mirrors `tests/test_blocker.py` cases)
- [x] `lock_manager.py` and `time_verifier.py` unit tests with a fake NTP client (`tests/test_lock_manager.py`, `tests/test_time_verifier.py`)
- [x] Coverage reporting in CI (`pytest --cov=daemon` + Codecov upload + README badge). One-time setup: enable the repo at codecov.io (install the Codecov GitHub app) for the badge/upload to work.

Known daemon/extension drift found while mirroring the tests (not yet reconciled):
- Path patterns: daemon uses plain `url.startswith(pattern)`, so `youtube.com/shortsfilm` matches `youtube.com/shorts` (extension correctly requires a path boundary) and `www.youtube.com/shorts` does NOT match (extension correctly handles subdomains on path patterns). Worth aligning the daemon to the extension's stricter parsing in a follow-up.

## 4. Packaging cleanup — DONE

- [x] Removed the `sys.path.insert` hack: `daemon/` is a proper package (`daemon/__init__.py`, `from daemon.x import y`, run as `python -m daemon.main`; watchdogs spawn `-m daemon.watchdog_runner`). Systemd unit (generated by `install.sh` and the installed copy) and tests updated; `tests/conftest.py` deleted in favor of `pythonpath = ["."]` in pyproject. The only remaining guard is a bootstrap shim in `watchdog_runner.py` for pre-package watchdog processes that respawn siblings by file path.
- [x] Collapsed to a **single root `pyproject.toml`** (the root file already duplicated every component dependency, so a uv workspace added ceremony for no benefit). Hatchling builds the `daemon` package; desktop/tray stay PyInstaller-bundled scripts. Per-component `pyproject.toml`/`uv.lock` removed; `install.sh` now does one `uv sync` and all components share the root `.venv`. The old `daemon/.venv`, `desktop-app/.venv`, `tray/.venv` can be deleted after the next daemon restart picks up the new unit.
- [x] Naming: fully renamed to `hyprblocker` (executables, systemd unit, config/data dirs, desktop entries, UI strings) with a reboot-cutover design, since the live daemon can't be stopped: `daemon/legacy_migration.py` moves `~/.config/website-blocker` → `~/.config/hyprblocker` on the first post-reboot start (defers + exits while the old daemon holds the port), a `website-blocker.service` shim forwards restarts from pre-rename watchdogs to `hyprblocker.service`, and `~/.local/share/website-blocker` is a compat symlink so browser-loaded extensions and running apps keep working. The native-messaging host (`com.hyprblocker.host`) and Firefox extension ID (`hyprblocker@hyprblocker.local`) are renamed too; old-name browser manifests coexist until the extension is reloaded.

  **Post-reboot cleanup: DONE (2026-07-08).** The reboot cutover completed cleanly — config migrated whole (no legacy remnants), `./install.sh` swept the shim unit, old bins, compat symlink, and old-name browser manifests; the extension was reloaded, and the old per-component venvs and old-name build outputs were deleted. The transition shims still in the codebase (`daemon/legacy_migration.py`, the `__package__` bootstrap in `watchdog_runner.py`) are inert on a migrated install and kept for any other pre-rename machine.

## 5. Frontend polish

- [ ] Fix the remaining ESLint warning in `src/pages/Settings.tsx` (wrap `loadBrowserEnforcementStatus` / `loadSafeSearchStatus` in `useCallback` and add them to the `useEffect` deps)
- [ ] Consider `--max-warnings 0` in the lint script once warnings are at zero, so CI catches regressions

## 6. Portfolio presentation (outside the repo)

- [ ] Write a short "case study" blog post or portfolio page: the cat-and-mouse security story (each bypass you found and how you closed it) is the narrative interviewers will remember. Good material already exists in `docs/TECHNICAL_CONCEPTS.md` and `docs/WATCHDOG.md`.
- [ ] Pin the repo on your GitHub profile
- [ ] Add a one-line project blurb + link on your resume; mention concrete specifics (FastAPI daemon, watchdog process mesh, NTP-verified locks, Manifest v3 extension with native messaging)

## 7. Nice-to-haves

- [ ] GitHub release (v0.1.0) with built artifacts once packaging is settled
- [ ] `CONTRIBUTING.md` — even a short one signals professionalism
- [ ] Architecture diagram as an image (draw.io/excalidraw) to complement the ASCII art
- [ ] Demo mode or seed data script so screenshots/demos are reproducible
